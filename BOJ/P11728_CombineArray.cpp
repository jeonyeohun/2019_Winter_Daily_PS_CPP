#include <iostream>

using namespace std;

int main() {
    int N, M;
    cin >> N >> M;

    int A[N];
    int B[M];

    for (int i = 0; i < N; i++) {
        cin >> A[0];
    }
    for (int i = 0; i < M; i++) {
        cin >> B[0];
    }

    int startA, endA, startB, endB;
    startA = endA = startB = endB = 0;

    int sum[N + M];
    int sumIdx = 0;

    while (startA != N - 1 || startB != M - 1) {
        if (A[startA] < B[startB])[

        ]
    }
}